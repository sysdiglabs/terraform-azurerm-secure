# This is a placeholder TFLint config file.
#
# Our CI runs: `tflint --recursive --chdir=modules --config=.tflint.hcl`.
# When TFLint descends into `modules/agentless-scanning/modules/`, it expects
# a `.tflint.hcl` file to exist in that directory as well.
#
# The actual rules/config are defined in `modules/.tflint.hcl`.
